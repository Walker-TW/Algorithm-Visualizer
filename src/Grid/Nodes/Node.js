import React from 'react';

const Node = () => {
  const getStyle = {
    width: '20px',
    height: '20px',
    border: '1px solid black',
  };

  return <div style={getStyle} />;
};

export default Node;
