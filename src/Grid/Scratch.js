function dijkstra(startNode, endNode, grid) {
  const potentialRoute = [];
  const unvisitedNodes = grid;

  const neareastNeighbour = unvisitedNodes.shift();

  while (neareastNeighbour !== Infinity) {}
}
