import React, { Component } from "react";
import { Navbar, Button } from "react-bootstrap";

export class Header extends Component {
  render() {
    return <Button variant="primary">Dijkstra</Button>;
  }
}

export default Header;
